const header=document.querySelector('.site-header'),bar=document.querySelector('.progress i'),toggle=document.querySelector('.menu-toggle'),mobile=document.querySelector('.mobile-nav');
const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;bar.style.width=`${max?scrollY/max*100:0}%`;header.classList.toggle('scrolled',scrollY>20)};addEventListener('scroll',update,{passive:true});update();
toggle.addEventListener('click',()=>{const open=toggle.classList.toggle('open');toggle.setAttribute('aria-expanded',open);mobile.classList.toggle('open',open);mobile.setAttribute('aria-hidden',!open)});mobile.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>toggle.click()));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('in-view');observer.unobserve(entry.target)}}),{threshold:.14});document.querySelectorAll('.reveal,.process-step').forEach(el=>observer.observe(el));
document.querySelectorAll('.service-card').forEach(card=>card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--x',`${e.clientX-r.left}px`);card.style.setProperty('--y',`${e.clientY-r.top}px`)}));
if(matchMedia('(pointer:fine)').matches&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const glow=document.querySelector('.cursor-glow'),system=document.querySelector('.system');addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px';const x=(e.clientX/innerWidth-.5)*12,y=(e.clientY/innerHeight-.5)*12;system.style.setProperty('--mx',x+'px');system.style.setProperty('--my',y+'px')})}
document.querySelector('#year').textContent=new Date().getFullYear();
